import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"


const search = () => {
  return (
    <SafeAreaView>
      <Text className="text-center">Travel page...</Text>
    </SafeAreaView>
  )
}

export default search

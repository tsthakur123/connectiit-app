import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const home = () => {
  return (
    <SafeAreaView>
      <Text className="text-center">Anonymous Chat Page...</Text>
    </SafeAreaView>
  )
}

export default home
